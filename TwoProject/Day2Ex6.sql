SELECT m.pizza_name, z.name AS pizzeria_name FROM person_order AS po JOIN person AS p ON po.person_id=p.id 
JOIN menu AS m ON po.menu_id=m.id JOIN pizzeria AS z ON m.pizzeria_id=z.id WHERE p.name IN ('Denis','Anna') ORDER BY pizza_name, pizzeria_name;